# 동료 평가 발표 정리

> 미션 A2-03 · AI 웹 개발 — 내 아이디어를 현실로, AI 웹 서비스 빌딩

---

## 1. 최종 결과물

**숙소 정보를 넣으면 촬영할 장면 목록을 만들어 주는 웹 서비스입니다.**

| | 내용 |
|---|---|
| 배포 주소 | **https://instay-beta.vercel.app/** |
| 저장소 | `github.com/Clay-pieup/Codyssey_Mission_02_web` (Public) |
| 프론트엔드 | `index.html`, `guide.html`, `css/style.css`(357줄), `js/guide.js`(213줄) — **순수 HTML/CSS/JS** |
| 백엔드 | `api/generate.py` (168줄) — Vercel Serverless Function (Python) |
| 사용한 API | Google Gemini (`gemini-3.5-flash-lite`) |
| 외부 라이브러리 | `requests` 하나 |
| 보너스 | 다크 모드 (보너스 2 — UX·측정 고도화) |
| 문서 | `README.md` 약 1,330줄 — 단위별 설계 근거와 라인별 해설 |
| 기획서 | `docs/서비스기획서.md` |

▶ **여기서 직접 써 보세요**

`https://instay-beta.vercel.app/guide.html`
숙소 유형을 고르고, 특징을 한 줄 적고, 버튼을 누르면 약 3초 만에 결과가 나옵니다.
**휴대폰으로 열어도 됩니다.** 그렇게 만들었습니다.

---

## 2. 프로젝트 개요

**무엇을 만들었나**

숙소 협찬 영상을 찍는 숏폼 크리에이터는 현장에서 "뭘 찍어야 하지"를 즉흥으로 정하는 경우가 많습니다.
그러다 보면 찍고 와서 빠진 컷이 보이고, 영상마다 구성이 제각각이라 성과를 비교할 수도 없습니다.

**촬영 나가기 전에 장면 목록을 손에 쥐게 하자**는 것이 이 서비스입니다.

**핵심은 브라우저와 서버가 한 번 왕복하는 것입니다.**

```
  [브라우저]  숙소 유형 / 특징 / 톤 입력 → 버튼
        │
        │  fetch('/api/generate')  ← POST, 본문에 JSON
        ▼
  [서버]  api/generate.py  (Vercel Serverless Function, Python)
        │   ① 입력 검사
        │   ② 환경 변수에서 API 키 꺼내기
        │   ③ 프롬프트 조립
        │
        │  requests.post(...)  ← POST
        ▼
  [Gemini]  촬영 가이드를 JSON으로 생성
        │
        │  {"scenes":[5], "caption":"", "hashtags":[5]}
        ▼
  [서버]  ④ 응답이 약속한 모양인지 검사
        │
        ▼
  [브라우저]  renderGuide() 가 화면에 그림
```

**브라우저는 Gemini를 직접 부르지 않습니다.**

직접 부르면 코드가 짧아집니다. 그런데 그러려면 **API 키를 브라우저 코드에 넣어야 합니다.**
브라우저 코드는 누구나 열어볼 수 있습니다. `F12` 한 번이면 끝입니다. 키가 그대로 노출됩니다.

그래서 브라우저는 **내 서버**에게만 말을 걸고, Gemini와 이야기하는 것은 서버가 합니다.
키는 서버 쪽 환경 변수에만 있고 브라우저는 그 존재조차 모릅니다.
→ **백엔드가 필요한 이유가 이것입니다.** 과제가 `api/` 폴더를 요구한 것도 같은 맥락입니다.

**왜 AI에게 JSON으로 달라고 했나**

그냥 물어보면 "해변가 풀빌라시군요! 이렇게 찍어보세요~" 같은 **문장**으로 옵니다.
사람이 읽기엔 좋지만, 이 글에서 "1번 컷", "캡션", "해시태그"를 각각 잘라내려면
매번 달라지는 문장을 상대로 규칙을 만들어야 합니다.

```json
{"scenes": ["...", "..."], "caption": "...", "hashtags": ["#...", "#..."]}
```

이렇게 받으면 `data.scenes` 한 줄로 장면 목록을 꺼내 화면의 정해진 자리에 넣을 수 있습니다.

---

## 3. 웹 용어 풀이

> 개발을 처음 하는 사람에게 설명한다면 이렇게 말하겠습니다.

### 먼저 — 웹페이지는 세 가지로 되어 있습니다

| | 담당 | 없으면 |
|---|---|---|
| **HTML** | 무엇이 **있는가** — 제목, 문단, 입력칸, 버튼 | 아무것도 없습니다 |
| **CSS** | 그것이 어떻게 **보이는가** — 색, 크기, 배치 | 흑백 글자만 줄줄이 |
| **JavaScript** | 무엇이 **일어나는가** — 누르면 반응하는 것 | 그림만 있고 아무 일도 안 일어남 |

**이 프로젝트에서는 이 셋을 일부러 따로 얹었습니다.**
2단계에서 HTML만 만들어 배포했고(흑백 글자만 보였습니다), 4단계에서 CSS를 얹었고, 5단계에서 JavaScript를 얹었습니다.
한꺼번에 만들었다면 무엇이 무엇을 하는지 구분할 기회가 없었을 겁니다.

---

### 프론트엔드와 백엔드

| | 어디서 도나 | 누가 볼 수 있나 |
|---|---|---|
| **프론트엔드** | 사용자의 **브라우저** 안 | **누구나** (F12로 코드 열람 가능) |
| **백엔드** | **서버** 위 | 아무도 못 봄 (결과만 봄) |

이 구분이 곧 **비밀을 어디에 둘 것인가**의 답입니다.
API 키처럼 남이 보면 안 되는 것은 **반드시 백엔드**에 둡니다.

---

### Serverless Function — "서버 없음"이 아닙니다

보통 웹 서비스는 서버 컴퓨터를 24시간 켜 둡니다. 요청이 없어도 켜져 있으니 그동안 비용이 나갑니다.

**서버리스**는 **함수 하나만 올려 두고, 요청이 올 때만 잠깐 실행**하는 방식입니다.
이름은 "서버가 없다"지만, **내가 서버를 관리하지 않는다**는 뜻입니다. Vercel이 대신 켜고 끕니다.

**주소는 폴더 구조가 정합니다.**

```
api/generate.py   →   https://instay-beta.vercel.app/api/generate
```

별도 설정 파일이 필요 없습니다. 그래서 이 프로젝트에는 `vercel.json` 이 없습니다.

```python
class handler(BaseHTTPRequestHandler):
    def do_POST(self):
```

클래스 이름은 **반드시 `handler`(소문자)** 여야 합니다. Vercel이 이 이름을 찾기로 약속돼 있어서, 다르면 함수가 아예 실행되지 않습니다.

---

### fetch — 브라우저가 서버에 말을 거는 명령

```js
const response = await fetch("/api/generate", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ stayType, features, tone })
});
```

| 부분 | 하는 일 | 비유 |
|---|---|---|
| `"/api/generate"` | 어느 창구인지 | 몇 번 창구 |
| `method: "POST"` | 무엇을 하러 왔는지 | "이거 처리해 주세요" |
| `headers` | 어떤 형식인지 알림 | 소포에 붙인 송장 |
| `body` | 실제 내용 | 소포 안의 물건 |

**`await` 가 붙은 이유** — `fetch`는 답을 즉시 주지 않습니다. 네트워크를 건너갔다 와야 하니까요.
대신 "언젠가 답이 온다"는 **약속**을 즉시 돌려줍니다.
`await` 는 **"그 약속이 지켜질 때까지 여기서 멈췄다가 다음 줄로 가라"** 는 뜻입니다.
없으면 아직 도착하지 않은 답을 화면에 그리려다 아무것도 안 나옵니다.

---

### GET과 POST — 이 프로젝트는 POST를 씁니다

| | GET | POST |
|---|---|---|
| 뜻 | "이거 **주세요**" (조회) | "이거 받아서 **처리해 주세요**" (생성) |
| 데이터 위치 | 주소 뒤에 붙임 (`?features=해변가`) | 본문(body)에 담음 |
| 주소·기록에 남나 | **남습니다** | 안 남습니다 |
| 길이 제한 | 있음 | 사실상 없음 |
| 브라우저가 캐시하나 | **합니다** | 안 합니다 |

**왜 POST인가** — 숙소 특징은 여러 줄이 될 수 있고, 그 내용이 그대로 AI 프롬프트에 들어갑니다.
그리고 이 요청은 무언가를 *가져오는* 게 아니라 **만들어 달라고 시키는** 일입니다.

> 캐시 항목도 실제 영향이 있습니다. GET이면 브라우저가 "같은 주소니 아까 그 답 쓰자"며 서버를 안 부를 수 있습니다. 매번 새 결과가 필요한 기능에서는 곤란합니다.

---

### 상태 코드 — 서버가 숫자로 답합니다

| 번호대 | 뜻 | 예 | 다시 시도할 가치 |
|---|---|---|---|
| 2xx | 성공 | 200 | — |
| **4xx** | **내 요청**이 잘못됨 | 400 형식 오류 · 401 인증 실패 · 429 호출 과다 | **없음** |
| **5xx** | **서버 쪽** 문제 | 500 내부 오류 · 502 · 504 | **있음** |

**이 구분이 실제로 진단에 쓰였습니다.**

배포가 제대로 됐는지 확인하려고 `/api/generate` 주소를 열어봤습니다.

| 받은 코드 | 뜻 | 판단 |
|---|---|---|
| **404** Not Found | "그런 주소 없음" | 함수가 배포되지 않았다 |
| **501** Not Implemented | "주소는 있는데 그 방식은 지원 안 함" | **함수가 살아 있다** |

`api/generate.py` 에는 `do_POST` 만 만들고 `do_GET` 은 안 만들었습니다.
그러니 주소창으로 열면(GET) 501이 나오는 것이 **정확한 반응**입니다.
주소를 한 번 두드려 보는 것만으로 "파일이 없다"와 "파일은 있는데 요청 방식이 다르다"가 갈렸습니다.

---

### 로컬 환경과 배포 환경 — `file:///` 와 `https://`

파일을 더블클릭해서 열면 주소창이 이렇습니다.

```
file:///C:/Users/.../guide.html
```

배포 후에는 이렇습니다.

```
https://instay-beta.vercel.app/guide.html
```

| | `file:///` | `https://` |
|---|---|---|
| 파일을 어디서 | 내 컴퓨터 디스크에서 직접 | 서버에 요청해서 |
| 누가 볼 수 있나 | 나만 | 주소를 아는 누구나 |
| **서버가 있나** | **없습니다** | 있습니다 |

**셋째 줄이 핵심입니다.** `file:///` 에는 서버가 아예 존재하지 않습니다.
그런데 AI 기능은 `fetch('/api/generate')` 로 **서버를 부릅니다.** 없는 서버를 부르니 무조건 실패합니다.

→ **AI 기능 확인은 반드시 배포 주소에서 해야 합니다.**
이걸 모르면 "내 컴퓨터에선 안 되는데 왜?"를 코드에서 찾다가 시간을 버립니다.
코드가 틀린 게 아니라 **실행되는 환경이 다른 것**입니다.

---

### API 키와 환경 변수

**API 키는 비밀번호와 같습니다.** 유출되면 남이 내 계정으로 AI를 불러 요금이 나옵니다.

```python
api_key = os.environ.get("GEMINI_API_KEY")
```

`os.environ` 은 **프로그램이 실행되는 환경에 미리 심어 둔 값**을 읽는 통로입니다.
값은 Vercel 설정 화면에 있고, 코드에는 "그 이름의 값을 가져와라"는 지시만 있습니다.

| 어디에 | 무엇이 |
|---|---|
| Vercel 환경 변수 | **실제 키 값** |
| 저장소 코드 | 키의 **이름**만 (`GEMINI_API_KEY`) |

- 코드에 키가 없으니 저장소가 Public이어도 안전합니다
- 키를 바꿀 때 코드를 고치지 않습니다 (Vercel에서 값만 교체 후 재배포)

**키를 헤더에 담았습니다.**

```python
headers={"x-goog-api-key": api_key}
```

Gemini는 주소 뒤에 붙이는 방식(`?key=...`)도 허용합니다. 그런데 **주소는 어디에나 기록됩니다** — 서버 접속 로그, 중간 장비, 브라우저 기록. 헤더에 담으면 그런 기록에 남지 않습니다. 같은 요청, 다른 위험도입니다.

**유출됐다면**
1. Google AI Studio에서 그 키를 **폐기**하고 새로 발급
2. Vercel 환경 변수를 새 키로 교체 후 재배포
3. 키가 커밋에 섞였다면 **파일을 지우는 것만으로는 부족합니다.** 과거 커밋을 되짚으면 값이 그대로 보입니다

→ 그래서 `.gitignore` 를 **첫 커밋에** 넣었습니다. 되돌릴 수 없는 것은 앞에서 막습니다.

---

## 4. 작업 도구 및 내용

**도구**

| 역할 | 도구 | 비고 |
|---|---|---|
| 편집기 | Cursor | **Tab 자동완성 껐습니다** |
| 프론트엔드 | HTML / CSS / JavaScript | **프레임워크 없음** (과제 제약) |
| 백엔드 | Python (Vercel Serverless Functions) | |
| 라이브러리 | `requests` 하나 | AI 호출용 |
| 배포 | Vercel (GitHub 연동 자동 배포) | |
| 버전 관리 | Git + GitHub | 브랜치 `main` 단일 |

> **브랜치를 나누지 않았습니다.** 이 미션의 요구는 "커밋 이력을 남긴다"까지이고, Vercel은 `main`을 배포 브랜치로 봅니다. 갈래를 만들면 "지금 배포된 게 어느 브랜치인가"를 계속 신경 써야 해서, 얻는 것 없이 헷갈릴 거리만 늡니다.

**진행 방식 — 10단계로 쪼갰습니다**

| 단위 | 한 일 | 이 단위가 없었다면 |
|---|---|---|
| U0 | 저장소·폴더·`.gitignore` | 키가 GitHub에 올라갔을 것 |
| U1 | 서비스 기획서 | 만들면서 "뭘 만들지"를 계속 고민했을 것 |
| U2 | HTML 뼈대 (CSS 없음) | HTML의 역할을 눈으로 못 봤을 것 |
| U3 | **첫 배포** (빈 화면 상태로) | 아래 ② 참고 |
| U4 | CSS + 반응형 | |
| U5 | AI 기능 화면 (**가짜 데이터**) | 아래 ① 참고 |
| U6 | 백엔드 함수 (AI 없이 메아리) | 화면 문제인지 AI 문제인지 못 갈랐을 것 |
| U7 | Gemini 연동 + 환경 변수 | |
| U8 | 실패 처리 3종 | |
| U9 | 문서·증빙 정리 | |

**단위마다 배포해서 확인하고 넘어갔습니다.** 그래서 "배포 후 수정·재배포"를 여덟 번 반복했습니다.

---

**핵심 설계 판단 네 가지**

**① 가짜 데이터의 "모양"을 진짜와 똑같이 맞췄습니다** ← 이 프로젝트 최대 성과

U5에서 AI를 붙이기 전에, 화면 코드를 먼저 완성했습니다. 데이터는 브라우저 안에서 만든 가짜였습니다.

```js
// U5 — 가짜
return { scenes: [ ...5개 ], caption: "...", hashtags: [ ...5개 ] };
```

이 모양을 U1 기획서에서 정한 **최종 JSON 구조와 동일하게** 만들어 뒀습니다.

그래서 U7에서 진짜 AI를 붙일 때, 데이터를 가져오는 부분만 바꾸고
**화면을 그리는 `renderGuide()` 는 한 줄도 고치지 않았습니다.**

| 단위 | 데이터 출처 | `renderGuide()` |
|---|---|---|
| U5 | 브라우저 안의 가짜 데이터 | 그대로 |
| U6 | 서버가 되돌려준 값 | 그대로 |
| U7 | Gemini가 만든 진짜 가이드 | **그대로** |

화면 그리는 코드가 **"데이터가 어디서 왔는지 모르게"** 만들어 둔 것이 핵심입니다.

**② 첫 배포를 세 번째 단위로 앞당겼습니다**

보통은 다 만들고 마지막에 배포합니다. 그렇게 하지 않은 이유가 둘입니다.

*하나.* 배포는 내 코드 말고도 여러 가지가 맞물려야 성공합니다 — 저장소 연결, 접근 권한, 빌드 설정, 폴더 위치, 환경 변수.
마지막에 한꺼번에 연결하면 화면이 안 뜰 때 **의심할 곳이 대여섯 군데로 늘어납니다.**
빈 화면 상태에서 먼저 성공시키면 "저장소 → Vercel 경로는 정상"이 확정되고, 이후 깨지는 것은 **직전에 바꾼 것**이 범인입니다.

*둘.* 앞서 말한 대로 `file:///` 에는 서버가 없습니다. **배포 주소가 없으면 U6부터는 확인할 방법 자체가 없습니다.**

**③ HTML → CSS → JavaScript를 따로 얹었습니다**

과제 목표 1번이 "HTML/CSS/JavaScript가 각각 어떤 역할을 하는지 설명할 수 있다"입니다.
셋을 한꺼번에 만들면 무엇이 무엇을 하는지 구분되지 않습니다.

U2에서 HTML만 만들었을 때 화면은 흑백 글자뿐이었습니다. **그게 HTML이 담당하는 전부**입니다.
U4에서 CSS 파일 하나를 연결하자 화면이 달라졌는데, **HTML은 한 글자도 바꾸지 않았습니다.** 늘어난 것이 CSS의 몫입니다.
U5에서 JavaScript를 넣자 버튼이 반응했습니다. 그게 JavaScript의 몫입니다.

→ 말이 아니라 **직접 본 경험**으로 답할 수 있게 됐습니다.

**④ 실패를 "어디까지 갔다가 실패했는가"로 나눴습니다**

| 어디서 멈췄나 | 무엇으로 알아채나 | 화면에 보이는 것 |
|---|---|---|
| **보내기 전** (입력이 비었다) | 우리가 직접 검사 (`.trim()`) | 입력칸 아래 붉은 안내 + 테두리 + 커서 이동 |
| **서버에 닿지 못함** | `TypeError` | "네트워크 연결을 확인해 주세요" |
| 닿았으나 **답이 늦음** | `AbortError` | "응답이 늦어지고 있습니다" |
| 서버가 **오류로 답함** | 서버가 준 `error` 문구 | "인증 실패" / "요청이 너무 많습니다" 등 |

전부 `except Exception` 하나로 묶고 "오류가 발생했습니다"라고 해도 화면은 돌아갑니다.
그렇게 하지 않은 이유는 **원인마다 사용자가 할 일이 다르기 때문**입니다.
지연이나 호출 과다는 "다시 시도"가 의미 있지만, 인증 실패는 몇 번을 눌러도 소용없습니다.

**`fetch` 에는 시간 제한 기능이 없습니다.** 서버가 영영 답하지 않으면 브라우저는 계속 기다립니다.
그래서 `AbortController`(요청을 도중에 취소하는 스위치)를 만들고 20초 뒤 스위치를 내리는 타이머를 붙였습니다.

**왜 20초인가** — 실측 응답이 약 3초였습니다. 그 6배 이상입니다.
5초로 잡으면 조금만 느려져도 멀쩡한 요청을 죽이고, 60초면 사용자가 1분을 하염없이 기다립니다.
**기준은 감이 아니라 실제 측정값에서 나옵니다.**

---

**AI가 없는 사실을 지어낸 문제 — 프롬프트로 고쳤습니다**

특징에 **`해변가`** 한 단어만 넣었는데 결과에 이런 것들이 나왔습니다.

> "객실 내 **개별 인피니티 풀**", "거실 **통창**", "**침대에 누워서** 오션뷰"

**인피니티 풀이 있다고 말한 적이 없습니다.** AI가 "해변가 풀빌라면 보통 이렇겠지"라고 채운 것입니다.
버그가 아니라 생성형 AI의 기본 성질입니다. 빈칸이 있으면 그럴듯한 것으로 메웁니다.

**서비스로 보면 심각합니다.** 크리에이터가 이 목록을 들고 갔는데 인피니티 풀이 없으면 5컷 중 1컷은 촬영 불가입니다.
"빠뜨리는 컷을 없애자"는 목적과 **정반대로, 없는 컷을 찾아 헤매게** 만듭니다.

프롬프트에 두 줄을 넣었습니다.

```
- 위에 적힌 "숙소 특징"에 없는 시설이나 공간은 절대 지어내지 마세요.
- 정보가 부족하면 어느 숙소에나 있는 공간으로 채우세요.
  (입구, 침실, 창가, 욕실, 조명, 주변 풍경, 체크인 순간 등)
```

**두 번째 줄이 핵심입니다.** 금지만 하면 AI가 쓸 재료가 없어져 5컷을 못 채웁니다.
막기만 하지 않고 **대안을 줬습니다.** 입구·침실·창가는 어떤 숙소에도 있으니 지어내는 게 아닙니다.

조치 후 같은 입력으로 다시 실행한 결과:

| | 조치 전 | 조치 후 |
|---|---|---|
| 3컷 | 객실 내 **개별 인피니티 풀** | **침실** 인테리어 |
| 4컷 | **침대에 누워** 오션뷰 통창 | **욕실** 세면대 |

없는 시설이 사라지고, **5컷은 그대로 채워졌고**, `해변가` 라는 입력은 여전히 반영됐습니다.

**왜 코드가 아니라 프롬프트를 고쳤나**
결과를 검사해 특정 단어를 거르는 방식도 가능했습니다. 그런데
**"인피니티 풀은 지어낸 것이고 통창은 아니다"를 코드가 판단할 수 없습니다.**
무엇이 지어낸 것인지 아는 것은 문맥을 읽는 일이고, 그건 AI가 할 일입니다.
→ 어느 쪽으로 고칠지는 **"그 판단을 규칙으로 적을 수 있는가"** 로 갈립니다.

---

**보너스 — 다크 모드, 그리고 재보다가 찾은 결함**

운영체제가 어두운 테마일 때 화면도 어두워지게 했습니다. **JavaScript는 한 줄도 쓰지 않았습니다.**

```css
@media (prefers-color-scheme: dark) {
  :root { --text: #f0f0f0; --bg: #16181a; ... }
}
```

이 블록이 하는 일은 **색 변수를 다시 정의하는 것뿐**입니다. 나머지 350여 줄은 전부 `var(--이름)` 으로 색을 쓰고 있어 한 줄도 고치지 않았습니다.
U4에서 "색을 바꿀 때 고칠 곳이 한 줄이어야 한다"며 변수로 모아 둔 설계가 여기서 값을 했습니다.

**토글 버튼을 만들지 않았습니다.** 사용자는 이미 자기 취향을 운영체제 설정에 말해 두었습니다. 그 답을 무시하고 화면에서 또 묻는 대신 이미 한 대답을 따랐습니다.

**PDF는 보너스 2에 "개선 효과를 확인하는 방법"까지 요구합니다.**
다크 모드는 "좋아 보인다"로 판단하기 쉽습니다. 느낌은 근거가 못 되므로 **대비비**를 쟀습니다.
대비비는 두 색이 얼마나 잘 구분되는지의 수치이고, 접근성 국제 기준(WCAG AA)은 일반 글자에 **4.5:1 이상**을 요구합니다.

다크 모드는 6.4~15.6:1로 전부 통과했습니다. **그런데 재는 김에 원래 있던 라이트 모드를 재봤더니 문제가 나왔습니다.**

| | 조치 전 | 판정 | 조치 후 |
|---|---:|---|---:|
| 라이트 강조색 `#e0565b` | **3.72:1** | **미달** | `#c9484d` → **4.65:1** 통과 |

3.72:1은 큰 글자에만 허용되는 수준입니다. 그런데 이 코랄색은 **`필수` 표시(13px)와 오류 안내(14px)**에도 쓰이고 있었습니다.
**가장 정확히 읽혀야 할 오류 문구가 가장 안 읽히는 색이었던 것입니다.**

화면상 인상은 거의 그대로입니다. 새 값이 원래 마우스오버 색과 비슷하기 때문입니다.

> **요점은 순서입니다.** 측정 → 문제 발견 → 개선 → 재측정.
> 다크 모드를 만들다가 **라이트 모드의 결함**을 찾았습니다. 재보지 않았다면 계속 몰랐을 겁니다.
> "보기 좋아졌다"는 확인이 아닙니다. 확인은 **바꾸기 전과 후에 같은 자를 대는 것**입니다.

---

## 5. 과제 체크리스트 및 수행 여부

**제출 패키지 5종 전부, 기능 요구 8항목 전부 완료.**

| 구분 | 결과 | 한 줄 근거 |
|---|:--:|---|
| 제출 패키지 5종 | **5/5** | 배포 URL / 저장소 / README / 기획서 / 증빙 |
| 기능 요구사항 8개 | **8/8** | README 「제출 체크리스트」 A~G에 항목별 대조 |
| 개발 환경 제약 | **충족** | 프레임워크 0개, 백엔드는 `api/` Python |
| 페이지·섹션 3개 이상 | **4개** | 홈 / 이렇게 씁니다 / 갤러리 / FAQ + 별도 페이지 |
| 반응형 | **충족** | 375px · 1280px 두 크기에서 확인, 스크린샷 보유 |
| AI 기능 | **1개** | 촬영 가이드 생성기 |
| 실패 처리 | **4종** | PDF 요구는 **1종 이상**. 3종 + 네트워크 오류까지 |
| API 키 노출 | **0건** | 코드·README·스크린샷 전부 |

**실패 처리를 말이 아니라 재현으로 확인했습니다**

| 시나리오 | 방법 | 결과 |
|---|---|---|
| 빈 입력 | 특징 칸을 비우고 실행 | 서버에 요청조차 안 감. 입력칸 아래 안내 + 테두리 강조 |
| 공백만 입력 | 스페이스만 넣고 실행 | 같은 처리 (`.trim()` 이 잡음) |
| 네트워크 끊김 | F12 → Network → **Offline** | "네트워크 연결을 확인해 주세요" + 요청이 `(failed)` 로 기록 |
| 키 미설정 | (설계상) 환경 변수 없음 | AI를 부르기 **전에** 중단, 안내 반환 |

▶ **시연 가능**: 개발자 도구를 Offline으로 바꾸고 버튼을 누르면 한국어 안내가 뜹니다.

**보너스 2(UX·측정 고도화)를 다크 모드로 수행했습니다.**

| | 보너스 | 결과 |
|---|---|---|
| 1 | 운영 자동화 / 데이터 저장 고도화 | 미수행 |
| 2 | UX·측정 고도화 (다크/인터랙션/분석 중 1개 이상) | **완료 — 다크 모드** |

보너스 1을 안 한 시간은 **실패 처리 3종 전부 구현**과 **문서화**에 썼습니다.
평가가 "코드 완성도"가 아니라 "설명할 수 있는가"를 본다고 판단했기 때문입니다.

**중간에 겪은 문제 두 가지**

| 문제 | 증상 | 원인 | 어떻게 찾았나 |
|---|---|---|---|
| 커밋이 조용히 실패 | 코드를 고쳤는데 배포에 반영 안 됨 | `.git/index.lock` 잠금 파일이 남아 커밋이 거부됨 | `git status --short` 의 **글자 위치**로 "대기줄에만 있고 확정은 안 됨"을 확인 |
| 배포됐다고 착각 | 화면이 정상처럼 보였음 | 실제로는 이전 버전이 배포돼 있었음 | GitHub 저장소의 파일을 직접 열어 대조 |

두 번째가 특히 배울 점이었습니다. 확인 항목 중 하나가 **"내가 입력한 값이 결과에 나타나는가"** 였는데,
이전 버전에서도 그게 통과했습니다.
→ **옛날 코드에서도 통과하는 항목은 증거가 되지 못합니다.** 새 코드에만 있는 표시를 하나 심어야 갈립니다.

---

## 6. 배운 점 3가지

### ①

*(직접 작성)*

### ②

*(직접 작성)*

### ③

*(직접 작성)*

> 쓸 거리 후보 — 실제로 겪은 것 중에서 고르면 됩니다.
> - 브라우저가 AI를 직접 못 부르는 이유(키 노출)를 알고 나서 백엔드가 왜 필요한지 이해된 것
> - 배포를 먼저 해 두니 문제가 생겼을 때 원인을 좁히기 쉬웠던 것
> - HTML만 있는 화면 → CSS 얹은 화면 → JS 얹은 화면을 순서대로 본 경험
> - 커밋이 조용히 실패한 것을 `git status` 읽는 법을 배워 찾아낸 것
> - AI가 없는 시설을 지어내는 걸 발견하고 프롬프트로 고친 것
> - `file:///` 로는 AI 기능이 절대 동작하지 않는다는 걸 알게 된 것
> - 다크 모드를 만들다가 대비를 재보고, 원래 쓰던 색이 접근성 기준 미달인 걸 발견한 것

---

## 부록. 용어 사전

> 이번 미션을 하면서 실제로 만난 말들만 모았습니다.
> **"뜻 → 비유 → 이 프로젝트에서 어디"** 순으로 정리했습니다.

---

### A. 웹페이지를 이루는 것

| 용어 | 뜻 | 비유 |
|---|---|---|
| **HTML** | 페이지에 **무엇이 있는지** 적는 언어 | **건물의 뼈대와 방 배치.** 어디가 거실이고 어디가 부엌인지 |
| **CSS** | 그것이 **어떻게 보이는지** 정하는 언어 | **인테리어.** 벽지 색, 가구 배치, 조명 |
| **JavaScript** | 사용자 행동에 **반응하게** 하는 언어 | **전등 스위치와 자동문.** 누르면 뭔가 일어납니다 |
| **태그** | HTML에서 내용을 감싸는 표시 (`<p>`, `<button>`) | **서류의 항목 칸.** "이름:", "주소:" 처럼 |
| **id** | 문서 안에서 **딱 하나뿐인** 이름표 | **주민등록번호.** 한 사람에 하나 |
| **class** | **여러 곳에 반복해 붙이는** 이름표 | **학년.** 여러 사람이 같은 값을 가집니다 |
| **선택자** | CSS가 "어디에 이 스타일을 적용할지" 고르는 방법 | **"3학년 전원 앞으로"** 같은 호명 |
| **DOM** | 브라우저가 HTML을 읽어 메모리에 만든 구조 | **설계도를 보고 머릿속에 그린 집.** JS는 이걸 고칩니다 |
| **폼(form)** | 사용자 입력을 묶어 제출하는 단위 | **신청서 한 장** |
| **반응형** | 화면 폭에 따라 배치가 달라지게 만드는 것 | **접이식 테이블.** 좁으면 접고 넓으면 폅니다 |
| **미디어 쿼리** | "화면이 이만큼 넓으면 이렇게" 라는 CSS 조건문 | **"비 오면 우산 펴라"** 같은 조건 |
| **뷰포트** | 브라우저가 페이지를 그리는 화면 영역 | **창틀.** 창이 작으면 보이는 범위도 작습니다 |
| **CSS 변수** | 색·크기 값에 이름을 붙여 한 곳에 모아 둔 것 | **교실 앞 시간표.** 한 곳만 고치면 전부 반영됩니다 |
| **대비비** | 두 색이 얼마나 잘 구분되는지의 수치 | **형광펜 색과 종이 색의 차이.** 노란 종이에 노란 펜은 안 보입니다 |
| **접근성** | 눈·손·기기가 다른 사람도 쓸 수 있게 만드는 것 | **계단 옆의 경사로.** 없어도 대부분은 쓰지만 누군가는 못 씁니다 |

> **이 프로젝트에서**: HTML만 있는 화면을 먼저 배포하고, CSS를 얹고, JS를 얹었습니다.
> `<meta name="viewport">` 한 줄이 없으면 반응형 CSS를 아무리 잘 짜도 모바일에서 축소돼 보입니다.

---

### B. 브라우저와 서버가 이야기하기

| 용어 | 뜻 | 비유 |
|---|---|---|
| **프론트엔드** | 사용자의 브라우저에서 도는 부분 | **매장 앞. 손님이 보는 곳** |
| **백엔드** | 서버에서 도는 부분 | **주방. 손님이 못 보는 곳** |
| **API** | 남의 서비스를 불러 쓰는 창구 | **주문 창구.** 정해진 양식으로 주문하면 결과가 나옵니다 |
| **엔드포인트** | 요청을 보낼 정확한 주소 | **몇 번 창구** |
| **fetch** | 브라우저가 서버에 요청을 보내는 명령 | **창구에 주문서 내밀기** |
| **GET** | 있는 것을 **조회**. 조건을 주소 뒤에 | **"메뉴판 주세요"** |
| **POST** | 내용을 보내 **새 결과를 만들게** 함 | **"이 재료로 요리해 주세요"** |
| **헤더** | 요청에 붙이는 이름표 (형식·신분) | **소포에 붙인 송장** |
| **본문(body)** | 요청의 실제 내용 | **소포 안의 물건** |
| **상태 코드** | 결과를 알리는 세 자리 숫자 | **성적표 등급** |
| **4xx** | **내 요청**이 잘못됨 | **주소 잘못 써서 반송.** 또 보내도 또 반송 |
| **5xx** | **서버 쪽** 문제 | **택배사 파업.** 좀 있다 다시 보내면 됩니다 |
| **JSON** | 컴퓨터끼리 쓰는 데이터 표기법 | **이름표 붙은 서랍장.** 어느 서랍에 뭐가 있는지 적혀 있음 |
| **비동기 / `await`** | 답이 올 때까지 기다렸다 다음 줄로 가는 것 | **주문하고 진동벨 기다리기.** 벨이 울려야 받으러 갑니다 |
| **타임아웃** | "이만큼 기다리다 답 없으면 포기" 하는 시간 제한 | **약속 장소에서 30분만 기다리기** |
| **CORS** | 다른 주소의 서버를 브라우저가 부를 때 걸리는 보안 규칙 | **출입증.** 같은 건물이면 안 필요합니다 |

> **이 프로젝트에서**: 브라우저는 **내 서버**(`/api/generate`)만 부르고, Gemini는 서버가 부릅니다.
> 둘 다 POST입니다. 배포 확인 때 404와 501의 차이가 진단 도구가 됐습니다.
> 주소가 같은 사이트 안(`/api/...`)이라 CORS 문제는 겪지 않았습니다.

---

### C. 배포와 운영

| 용어 | 뜻 | 비유 |
|---|---|---|
| **배포(deploy)** | 만든 것을 인터넷에 올려 남이 쓰게 하는 것 | **가게 문 열기** |
| **호스팅** | 내 파일을 대신 보관하고 보여주는 서비스 | **임대 매장** |
| **Vercel** | GitHub와 연결하면 자동 배포해 주는 호스팅 | **택배 자동 발송 계약.** 물건 넣어두면 알아서 보냅니다 |
| **Serverless Function** | 요청이 올 때만 잠깐 실행되는 함수 | **필요할 때만 부르는 출장 요리사.** 상주 셰프가 아닙니다 |
| **빌드** | 사람이 쓴 코드를 브라우저가 읽을 형태로 바꾸는 작업 | **번역.** 원문 그대로 읽을 수 있으면 필요 없습니다 |
| **`requirements.txt`** | 백엔드가 쓰는 파이썬 패키지 목록 | **장보기 목록.** 이대로 사면 같은 요리가 됩니다 |
| **버전 고정** | 패키지 버전을 정확히 못 박는 것 (`==2.32.3`) | **같은 브랜드 같은 용량으로 사기.** 아무거나 사면 맛이 달라집니다 |
| **로컬 환경** | 내 컴퓨터에서 도는 상태 | **집 주방** |
| **배포 환경** | 서버에서 도는 상태 | **실제 영업 주방.** 설비가 다릅니다 |
| **재배포** | 코드를 고친 뒤 다시 올리는 것 | **메뉴 바꾸고 다시 여는 것** |

> **이 프로젝트에서**: 빌드가 없습니다. 프레임워크를 안 써서 브라우저가 바로 읽는 HTML을 직접 썼기 때문입니다.
> `git push` 하면 Vercel이 자동으로 다시 배포합니다. 이 흐름을 여덟 번 반복했습니다.

---

### D. 비밀을 지키는 법

| 용어 | 뜻 | 비유 |
|---|---|---|
| **API 키** | "이 요청은 내 것"임을 증명하는 비밀 문자열 | **집 열쇠.** 남이 주우면 들어옵니다 |
| **환경 변수** | 프로그램 바깥에 따로 두는 설정값 | **서랍에 넣어둔 비밀번호 쪽지.** 교과서에 안 적습니다 |
| **하드코딩** | 값을 코드에 직접 박아 넣는 것 (키에는 금지) | **비밀번호를 교과서 표지에 적기** |
| **`.gitignore`** | "이 파일은 GitHub에 올리지 마라" 목록 | **일기장에 붙인 '보지 마세요' 스티커** |
| **커밋 이력** | 지금까지의 변경 기록 전부 | **CCTV 녹화본.** 지운 것도 되돌려 볼 수 있습니다 |
| **쿼터(한도)** | 하루/한 달에 쓸 수 있는 횟수 제한 | **자유이용권 횟수 제한** |
| **XSS** | 사용자가 넣은 글자가 코드로 실행되는 공격 | **주문서에 "금고 열어줘"라고 적었는데 직원이 그대로 실행** |

> **이 프로젝트에서**: 키는 Vercel 환경 변수에만 있고, 코드에는 **이름만** 있습니다.
> 화면에 글자를 넣을 때 `innerHTML` 이 아니라 `textContent` 를 씁니다.
> `innerHTML` 은 받은 것을 **HTML 명령으로 실행**해서, 사용자가 넣은 글이 코드가 될 수 있습니다.

---

### E. 이 프로젝트에서 만든 규칙

발표 중 "왜 그렇게 했나"를 물으면 이 문장들로 답할 수 있습니다.

| 규칙 | 한 줄 |
|---|---|
| 비밀은 브라우저에 둘 수 없다 | 브라우저 코드는 F12 한 번이면 누구나 봅니다 |
| 가짜 데이터도 모양은 진짜와 같게 | 그래야 나중에 진짜로 바꿀 때 화면 코드를 안 고칩니다 |
| 통로를 먼저 뚫고 내용물은 나중에 | 화면 문제인지 AI 문제인지 갈라야 원인을 찾습니다 |
| 되돌릴 수 없는 것은 앞에서 막는다 | `.gitignore` 를 첫 커밋에 넣은 이유입니다 |
| 오류는 발생한 지점에서 잡는다 | 그냥 두면 엉뚱한 줄에서 터져 진짜 원인이 가려집니다 |
| 실패는 "어디까지 갔다가"로 나눈다 | 원인마다 사용자가 할 수 있는 일이 다릅니다 |
| 기준은 감이 아니라 측정값에서 | 응답이 3초라 타임아웃을 20초로 잡았습니다 |
| 막을 거면 대안도 준다 | "지어내지 마라"만으로는 5컷을 못 채웁니다 |
| 판단을 규칙으로 적을 수 없으면 AI에게 맡긴다 | 무엇이 지어낸 것인지는 코드가 판단 못 합니다 |
| 옛 코드에서도 통과하는 항목은 증거가 아니다 | 새 코드에만 있는 표시를 심어야 갈립니다 |
| 확인은 느낌이 아니라 같은 자를 대는 것 | 대비를 재봤더니 원래 있던 색이 기준 미달이었습니다 |
